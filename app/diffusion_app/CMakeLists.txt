
SET(TARGET_SRC main.cpp)

ADD_EXECUTABLE(${APPDIFFUSION} ${TARGET_SRC})
DEAL_II_SETUP_TARGET(${APPDIFFUSION})

# link against the runner library
TARGET_LINK_LIBRARIES(${APPDIFFUSION} pfsim ${DIFFUSIONLIB})

TARGET_INCLUDE_DIRECTORIES(${TARGET} PRIVATE ${PROJECT_SOURCE_DIR}/include)