# compile the actual FE program as a library

ADD_LIBRARY(${DIFFUSIONLIB} diffusion.cpp)
DEAL_II_SETUP_TARGET(${DIFFUSIONLIB})
TARGET_LINK_LIBRARIES(${DIFFUSIONLIB} pfsim)
TARGET_INCLUDE_DIRECTORIES(${DIFFUSIONLIB} PRIVATE ${PROJECT_SOURCE_DIR}/include )


